import 'package:flutter/material.dart';

class categoryFunction extends StatelessWidget {
  const categoryFunction({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
